'use strict';

// const Request = require('../lib/notes');

// describe('notes file tests', () => {
//     it('is an object', () => {
//         if( Request === 'object' ) {
//             return true;
//         }
//         else {
//             return false;
//         }
//     })
// })
let newObj = [];

describe('test obj', () => {
    it('is an object', () => {
        let type = whatIs(newObj);
        
    })
})



